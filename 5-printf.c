#include <stdio.h>
/**
 * main - Entry point
 * My hello ,print task
 * Return: return 1 after program excecution
 */
int main(void)
{
	print f("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
